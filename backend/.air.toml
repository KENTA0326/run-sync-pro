root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/main"
  cmd = "CGO_ENABLED=0 go build -p 1 -ldflags='-s -w' -o ./tmp/main ."
  delay = 1000
  exclude_dir = ["assets", "tmp", "vendor"]
  include_ext = ["go", "tpl", "tmpl", "html"]
  stop_on_error = true

[screen]
  clear_on_rebuild = true